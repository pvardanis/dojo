---
phase: 02-domain-application-spine
plan: 05
subsystem: test-gates
tags:
  - test-03
  - contract-test
  - import-linter
  - boundary
  - ci
  - phase-close
requirements:
  - TEST-03
dependency-graph:
  requires:
    - "02-01"
    - "02-02"
    - "02-03"
    - "02-04"
  provides:
    - contract-harness
    - boundary-enforcement
  affects:
    - make-check
    - pre-commit
tech-stack:
  added:
    - "import-linter>=2.0 (grimp-backed boundary checker)"
  patterns:
    - "parametrised contract fixture with double-gate auto-skip"
    - "forbidden-module contracts in pyproject.toml"
key-files:
  created:
    - tests/contract/__init__.py
    - tests/contract/test_llm_provider_contract.py
  modified:
    - pyproject.toml
    - Makefile
    - uv.lock
decisions:
  - "Anthropic leg auto-skips via pytest.importorskip — no Phase-2 stub needed; Phase 3 activates it by creating app.infrastructure.llm.anthropic_provider."
  - "Boundary enforcement uses two forbidden contracts (not one layered contract) to keep failure messages pointed and allow domain/application to be analysed independently."
  - "Negative-path proof is local verification only — not committed — because the purpose is to prove the gate enforces, not to persist a broken tree."
metrics:
  duration: "~15 min (2 commits + negative-path proof + SUMMARY)"
  completed: "2026-04-23"
  tasks: 2
  files_created: 2
  files_modified: 3
  tests_added: 2
  make_check_seconds: 2.48
---

# Phase 2 Plan 05: Contract Harness + Import-Linter Summary

**One-liner**: TEST-03 contract harness over `[fake, anthropic]` with
double-gate auto-skip on the real leg, plus import-linter forbidden
contracts wired into `make lint` to enforce the DIP boundary — closing
Phase 2 in full.

## Files Created

| File                                           | Lines | Purpose                                           |
| ---------------------------------------------- | ----- | ------------------------------------------------- |
| `tests/contract/__init__.py`                   |   3   | Package marker (ABOUTME + module docstring)       |
| `tests/contract/test_llm_provider_contract.py` |  38   | Parametrised fixture + shape contract test        |

## Files Modified

| File            | Change                                                      |
| --------------- | ----------------------------------------------------------- |
| `pyproject.toml`| +1 dev dep (`import-linter>=2.0`); +14 lines `[tool.importlinter]` block with two forbidden contracts |
| `Makefile`      | `lint:` extended from 1 to 2 commands (ruff then lint-imports) |
| `uv.lock`       | Regenerated by `uv sync` — adds `import-linter==2.11` + `grimp==3.14` + transitive (`rich`, `markdown-it-py`, `mdurl`) |

## Contract-Harness Behavior Snapshot

Both invocations yield identical shape in Phase 2 (adapter module
absent):

```
$ uv run pytest tests/contract/test_llm_provider_contract.py -v
tests/contract/test_llm_provider_contract.py::test_generate_returns_note_and_card_list[fake] PASSED
tests/contract/test_llm_provider_contract.py::test_generate_returns_note_and_card_list[anthropic] SKIPPED
SKIPPED [1] tests/contract/test_llm_provider_contract.py:30: RUN_LLM_TESTS not set
========================= 1 passed, 1 skipped in 0.06s =========================

$ RUN_LLM_TESTS=1 uv run pytest tests/contract/test_llm_provider_contract.py -v
tests/contract/test_llm_provider_contract.py::test_generate_returns_note_and_card_list[fake] PASSED
tests/contract/test_llm_provider_contract.py::test_generate_returns_note_and_card_list[anthropic] SKIPPED
SKIPPED [1] tests/contract/test_llm_provider_contract.py:30: could not import 'app.infrastructure.llm.anthropic_provider': No module named 'app.infrastructure.llm'
========================= 1 passed, 1 skipped in 0.05s =========================
```

The skip reason changes (env gate vs. import gate) but the count stays
`1 passed, 1 skipped` either way — exactly the contract in CONTEXT D-11.

## pyproject.toml Diff (brief)

**Dev deps** — inserted after `interrogate>=1.7`:

```toml
    "import-linter>=2.0",      # Plan 02-05: domain/app boundary enforcement
```

**Appended after `[tool.ty]` block**:

```toml
[tool.importlinter]
root_package = "app"

[[tool.importlinter.contracts]]
name = "Domain must not depend on infrastructure or web"
type = "forbidden"
source_modules = ["app.domain"]
forbidden_modules = ["app.infrastructure", "app.web"]

[[tool.importlinter.contracts]]
name = "Application must not depend on infrastructure or web"
type = "forbidden"
source_modules = ["app.application"]
forbidden_modules = ["app.infrastructure", "app.web"]
```

## Makefile Diff (brief)

```diff
 lint:
 	uv run ruff check --fix .
+	uv run lint-imports
```

`check:` target unchanged — it already calls `lint`, so the new step
flows into `make check` automatically.

## Negative-Path Evidence

Injected `from app.infrastructure.db.session import engine` at the end
of `app/domain/entities.py` (local only, not committed). `uv run
lint-imports` exited **1**, naming both broken contracts and the
offending direct import plus the transitive leak through
`app.application.ports`:

```
Domain must not depend on infrastructure or web BROKEN
Application must not depend on infrastructure or web BROKEN

Contracts: 0 kept, 2 broken.

Domain must not depend on infrastructure or web
-----------------------------------------------
app.domain is not allowed to import app.infrastructure:
-   app.domain.entities -> app.infrastructure.db.session (l.74)
```

After `git checkout app/domain/entities.py`, `lint-imports` returned to
`Contracts: 2 kept, 0 broken` with exit 0. Full log at
`/tmp/lint-imports-violation.log` (local artifact; not committed).

This demonstrates the gate catches both direct and transitive boundary
leaks — import-linter walks the full dependency graph per RESEARCH
§1.1b.

## `make check` End-to-End Timing

```
make check  2.24s user 0.32s system 103% cpu 2.482 total
```

Covers: format → lint (ruff + lint-imports) → typecheck → docstrings →
docparams → pytest (94 passed, 1 skipped). `lint-imports` itself is
sub-second on Dojo's 22-file tree — matches RESEARCH §1.1c expectation.

## Acceptance Criteria Verification

| Criterion                                                                 | Status |
| ------------------------------------------------------------------------- | ------ |
| `tests/contract/__init__.py` + `test_llm_provider_contract.py` exist       | YES (41 lines total) |
| Harness contains literal fixture params + skip strings per PLAN            | YES    |
| `uv run pytest tests/contract/...` → `1 passed, 1 skipped`                 | YES    |
| `RUN_LLM_TESTS=1 uv run pytest tests/contract/...` → `1 passed, 1 skipped` | YES    |
| Harness file ≤100 lines                                                    | YES (38) |
| `uv run ruff check tests/contract/` exits 0                                | YES    |
| No `Mock`/`MagicMock`/`AsyncMock` in harness                               | YES (grep clean) |
| `grep -c "\[\[tool\.importlinter\.contracts\]\]" pyproject.toml` → `2`     | YES    |
| `Makefile` `lint:` has both ruff and lint-imports steps                    | YES    |
| `uv run lint-imports` exits 0                                              | YES (2 kept, 0 broken) |
| Deliberate violation makes `lint-imports` exit non-zero                    | YES (exit 1, both contracts BROKEN) |
| `make check` exits 0 end-to-end                                            | YES (2.48s) |
| Final Phase-2 regression ≥50 passed, 1 skipped                             | YES (84 passed, 1 skipped) |

## TDD Log

Per Dojo's `feedback_tdd_commit_convention` — RED and GREEN merge in
one commit (pre-commit runs pytest, which blocks RED-only commits). The
TDD discipline is recorded here and in the dev loop.

### Task 1 — TEST-03 contract harness

- **RED substitute** (negative verification): After creating the
  harness, broke `FakeLLMProvider.generate_note_and_cards` to return
  `(note, "not a list")`. Ran pytest — `test_generate_returns_note_and_card_list[fake]`
  failed on `assert isinstance(cards, list)`. This proves the harness
  catches real drift, serving the role of a "would-fail-without-the-fix"
  check since the harness itself can't meaningfully fail in isolation
  (the fake already existed from Plan 03).
- **GREEN**: Reverted the break via `git checkout tests/fakes/fake_llm_provider.py`.
  Reran pytest — back to `1 passed, 1 skipped`. Committed as
  `test(02-05): add TEST-03 contract harness for LLMProvider` (`af53016`).
- **REFACTOR**: Not needed — the harness is the minimal shape from
  PATTERNS.md, already ruff-clean and ≤100 lines.

### Task 2 — Import-linter wiring

- **RED substitute** (negative-path proof): Appended `from
  app.infrastructure.db.session import engine` to
  `app/domain/entities.py`. `uv run lint-imports` exited 1 with both
  contracts BROKEN, naming `app.domain.entities ->
  app.infrastructure.db.session` as the direct violation and exposing
  the transitive leak through `app.application.ports`. This IS the
  "failing test" — a contract that correctly detects a violation.
- **GREEN**: `git checkout app/domain/entities.py` restored the tree;
  `lint-imports` returned exit 0 with `Contracts: 2 kept, 0 broken`.
  `make check` green end-to-end. Committed as
  `feat(02-05): wire import-linter boundary enforcement into make lint`
  (`d423328`).
- **REFACTOR**: Not needed — the `[tool.importlinter]` block is the
  minimal two-contract form from CONTEXT D-12, and `lint:` is the
  two-line form from PATTERNS.md.

## Phase-Close Confirmation

**TEST-03 discharged**: TEST-03 contract harness is in `tests/contract/`
and runs on every `make check`. Phase 3's `AnthropicLLMProvider` will
slot into the parametrised fixture with zero further changes.

**Phase-1 LEARNINGS open item closed**: "Phase-2 boundary lint" is now
enforced by import-linter in `make lint` (and therefore `make check`
and pre-commit). This item should be marked closed in the Phase 2
LEARNINGS.md at phase close.

**Phase 2 SC #1-#6 all satisfied**:

- SC #1 (Protocol/Callable ports in `app/application/ports.py`): Plan 02.
- SC #2 (DTOs in `app/application/dtos.py` + domain entities pure-data): Plans 01-02.
- SC #3 (hand-written fakes, one per port, no `Mock()`): Plan 03.
- SC #4 (`GenerateFromSource` use case TOPIC end-to-end): Plan 04.
- SC #5 (contract-test harness parameterised over `[FakeLLMProvider, AnthropicLLMProvider]`, gated on `RUN_LLM_TESTS=1`): **this plan**.
- SC #6 (`import-linter` configured; DIP boundary violation fails `make check`): **this plan**.

**Phase-2 requirements discharged**:

- **DRAFT-01**: In-memory draft store (Plan 03).
- **TEST-01**: Unit tests per layer, hand-written fakes at every DIP boundary (Plans 01-04).
- **TEST-03**: Contract-test harness shared across fake + real LLM impls (this plan).

Phase 2 is complete pending the orchestrator's STATE.md / ROADMAP.md
update and the plan's PR merge.

## Self-Check: PASSED

- FOUND: `tests/contract/__init__.py` (3 lines)
- FOUND: `tests/contract/test_llm_provider_contract.py` (38 lines)
- FOUND: `import-linter>=2.0` in `pyproject.toml` dev deps
- FOUND: 2 `[[tool.importlinter.contracts]]` blocks in `pyproject.toml`
- FOUND: `uv run lint-imports` line in `Makefile` `lint:` target
- FOUND: commit `af53016` (`test(02-05): add TEST-03 contract harness for LLMProvider`)
- FOUND: commit `d423328` (`feat(02-05): wire import-linter boundary enforcement into make lint`)
- FOUND: `make check` exits 0 end-to-end (94 passed, 1 skipped, 2.48s)
- FOUND: negative-path proof captured (exit 1 with both contracts BROKEN; log at `/tmp/lint-imports-violation.log`, evidence snippet above)
