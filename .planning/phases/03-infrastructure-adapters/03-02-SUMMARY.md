---
phase: 03-infrastructure-adapters
plan: 02
subsystem: database
tags: [python, sqlalchemy, alembic, orm, mappers, sqlite, phase-3]

# Dependency graph
requires:
  - phase: 01-project-scaffold-tooling
    provides: "Base (DeclarativeBase), SessionLocal, empty 0001 alembic baseline, env.py settings-aware wiring, tmp-SQLite + _migrated_engine fixtures"
  - phase: 02-domain-application-spine
    provides: "Source/Note/Card/CardReview frozen dataclasses; SourceKind/Rating StrEnums; SourceId/NoteId/CardId/ReviewId NewType aliases"
provides:
  - "Four Mapped[...] ORM row classes (SourceRow, NoteRow, CardRow, CardReviewRow) inheriting from the existing Base"
  - "Eight pure mapper functions ({source,note,card,card_review}_{to_row,from_row}) that round-trip every domain field (UUID↔str, enum↔str, tags tuple↔JSON-encoded str)"
  - "First real Alembic migration (0002) creating sources/notes/cards/card_reviews tables on top of the empty 0001 baseline"
  - "M9 mitigation: env.py eager-imports app.infrastructure.db.models so Base.metadata.tables is populated at autogen time"
  - "Upgrade → downgrade → upgrade Alembic round-trip integration test"
  - "Base.metadata.tables smoke test (R5 / M9 defense)"
affects:
  - "03-03 (SQL repositories)"
  - "04 (routes + SaveDraft use case)"
  - "05 (composition root + arch docs)"

# Tech tracking
tech-stack:
  added: []   # no new runtime dependencies — all stack already pinned in Phase 1
  patterns:
    - "Declarative ORM row classes with Mapped[...] annotations (SQLAlchemy 2.0)"
    - "Pure module-level mapper functions (domain ↔ row) — no classes, no state, unit-testable without DB"
    - "Eager-import of ORM models from migrations/env.py to populate Base.metadata before autogenerate (M9 mitigation)"
    - "Alembic revision 0002 stacked on empty 0001 baseline; hash-style revision-id replaced with ordered strings (0002/0001)"

key-files:
  created:
    - "app/infrastructure/db/models.py"
    - "app/infrastructure/db/mappers.py"
    - "migrations/versions/0002_create_initial_schema.py"
    - "tests/unit/test_mappers.py"
    - "tests/integration/test_alembic_metadata.py"
    - "tests/integration/test_alembic_round_trip.py"
  modified:
    - "migrations/env.py"
    - "tests/integration/test_alembic_smoke.py"

key-decisions:
  - "D-02d column types locked: ids as Mapped[str] (UUID↔str at mapper boundary), timestamps as Mapped[datetime], enums as Mapped[str] (.value↔Enum at mapper), Card.tags as Mapped[str] JSON-encoded"
  - "D-02b: no ORM relationship() calls — flat per-table shape matches Phase 2 port flatness; sidesteps PITFALLS C1/C2"
  - "D-08a: one migration for all four tables (atomic schema delivery, not per-entity migrations)"
  - "R5: env.py eager-imports models (Option A in RESEARCH) rather than hiding it in db/__init__.py — explicit import at the call site that cares"
  - "Mappers kept as one flat file (116 LOC) — under the 150 hard-limit in CLAUDE.md; splitting into per-entity files would trade one file for five with no additional clarity"
  - "Round-trip test explicitly requests _migrated_engine fixture to guarantee session-scoped upgrade runs before the test body — removes test-order dependency"

patterns-established:
  - "Two-line ABOUTME headers on every new file"
  - "SQLAlchemy 2.0 Mapped[...] column annotations inheriting from the live Base in session.py"
  - "Mapper functions are pure and live alongside models in app/infrastructure/db/; unit-tested without any DB fixture"
  - "Alembic revisions use string ids matching the 0001_initial.py style (revision: str = \"NNNN\", down_revision: str | None = \"PREV\")"

requirements-completed: [PERSIST-02]

# Metrics
duration: ~15 min
completed: 2026-04-24
---

# Phase 3 Plan 02: ORM models + mappers + initial schema Summary

**Four Mapped[...] SQLAlchemy row classes, eight pure mapper functions (UUID↔str, enum↔str, tags tuple↔JSON), and the first real Alembic migration (0002) stacked on Phase 1's empty 0001 baseline — unblocking Plan 03's SQL repositories.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-04-24T13:25:00Z
- **Completed:** 2026-04-24T13:40:00Z
- **Tasks:** 4
- **Files created:** 6
- **Files modified:** 2

## Accomplishments

- Persistence-schema foundation landed: 4 Mapped[...] row classes + 8 pure mapper functions.
- First real Alembic migration (0002) — 4 create_table / 4 drop_table, autogenerated then human-reviewed per CONTEXT D-08.
- M9 "empty migration" pitfall mitigated: env.py eagerly imports models, and a 5-line metadata smoke test asserts Base.metadata.tables == {sources, notes, cards, card_reviews}.
- Upgrade → downgrade → upgrade round-trip integration test guards against future SQLite-specific ALTER-TABLE regressions.
- `make check` green throughout (102 tests pass, 1 LLM-opt-in skip); import-linter kept (domain still has zero infrastructure imports).

## Task Commits

Each task was committed atomically (`--no-verify` per worktree parallel-execution rules):

1. **Task 1: ORM row classes + env.py eager-import + metadata smoke test** — `cd20962` (feat)
2. **Task 2: Pure mapper functions + unit round-trip tests** — `9bb98f5` (feat)
3. **Task 3: Alembic 0002 migration + upgrade/downgrade round-trip** — `148ac67` (feat)
4. **Task 4: SUMMARY.md** — committed below after this SUMMARY is written.

## Files Created/Modified

**Created:**
- `app/infrastructure/db/models.py` (61 LOC) — SourceRow, NoteRow, CardRow, CardReviewRow with Mapped[...] annotations; no relationship() calls; no is_correct column (derived @property lives on the domain entity).
- `app/infrastructure/db/mappers.py` (116 LOC) — 8 pure module-level functions that round-trip domain entities ↔ row classes. UUID↔str, enum↔str via `.value`/`Enum(str)`, tags↔JSON via json.dumps/loads.
- `migrations/versions/0002_create_initial_schema.py` (70 LOC) — 4 op.create_table / 4 op.drop_table calls. Columns: String for ids/enums/tags (D-02d), DateTime for timestamps. PKs only; no indexes or alter_column calls.
- `tests/unit/test_mappers.py` (91 LOC) — 6 round-trip tests: Source (TOPIC + FILE kinds), Note, Card (with tags + empty tags), CardReview (including derived is_correct property).
- `tests/integration/test_alembic_metadata.py` (18 LOC) — R5/M9 smoke asserting Base.metadata.tables equals exactly the four expected names after importing the models module.
- `tests/integration/test_alembic_round_trip.py` (43 LOC) — upgrade → downgrade → upgrade cycle on the tmp SQLite DB. Requests _migrated_engine so the session-scoped upgrade runs first; restores head at the end to keep test order-independent.

**Modified:**
- `migrations/env.py` — added `from app.infrastructure.db import models as _models  # noqa: F401 (M9)` after the existing Base import. Ruff's import-sort placed it before the Base line; the effective behavior is identical because models.py itself imports Base.
- `tests/integration/test_alembic_smoke.py` — Rule 1 auto-fix: head revision assertion updated from `"0001"` to `"0002"` (direct consequence of 0002 becoming the new head).

## Decisions Made

All decisions were discharged from CONTEXT.md or RESEARCH.md:

- **D-02 / D-02d:** declarative Mapped[...] row classes + pure mapper functions; column types locked (ids/enums/tags as str, timestamps as datetime).
- **D-02b:** zero ORM relationships — grep confirms `relationship(` appears only in a comment explaining the rule.
- **D-08 / D-08a:** one migration for all four tables; autogenerate → human-review → edit workflow. Autogen output was clean (no alter_column, no indexes, no out-of-order create_index) so the review edit-pass only normalized the revision id to `"0002"` and added the ABOUTME/docstring header to match `0001_initial.py` style.
- **R5 / M9:** env.py eager-imports models (Option A from RESEARCH §R5) rather than hiding the side-effect in `db/__init__.py`.

Judgment call: `mappers.py` landed at 116 LOC, above the 100-line soft-preference but below the 150-line hard-limit in CLAUDE.md. Kept flat because splitting into five files (one per entity + `__init__.py` re-exporting 8 names) would trade one file for five without clarity gain — the content is eight three-line functions with identical shape. The plan anticipated both options; flat stays within CLAUDE.md's "≤100 lines (split if >150)" rule.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `test_alembic_smoke.py` head-revision assertion stale after 0002 landed**
- **Found during:** Task 3 (after `make check`)
- **Issue:** The Phase 1 smoke test hardcoded `assert version == "0001"` as the head revision. Adding 0002 made that assertion fail.
- **Fix:** Updated to `assert version == "0002"`. Test still exercises the same "upgrade head on fresh DB → version table present" pipeline, now tracking the new head.
- **Files modified:** `tests/integration/test_alembic_smoke.py`
- **Verification:** `uv run make check` green (102 passed, 1 LLM-opt-in skipped).
- **Committed in:** `148ac67` (Task 3 commit)

**2. [Rule 1 - Bug] round-trip test depended on implicit fixture ordering**
- **Found during:** Task 3 (running the new test in isolation)
- **Issue:** The plan's sample test body assumed `_migrated_engine` had already run by the time `test_upgrade_downgrade_upgrade_cycle` started. When pytest collected only this test, the fixture hadn't been requested and the tables weren't yet created — first assertion failed with `set()` on the left.
- **Fix:** Added `_migrated_engine: Engine` as an explicit fixture parameter so the session-scoped upgrade is guaranteed to run before the test body. Documented the restore-at-end contract in the docstring.
- **Files modified:** `tests/integration/test_alembic_round_trip.py`
- **Verification:** Test passes both in isolation and as part of the full suite; `make check` green.
- **Committed in:** `148ac67` (Task 3 commit)

**3. [Post-ruff auto-fix] import-sort reordering in migrations/env.py**
- **Found during:** Task 2 (`make check` lint stage)
- **Issue:** Ruff's `I001` wanted the two `app.infrastructure.db.*` imports sorted alphabetically — `models` before `session`.
- **Fix:** `uv run ruff check --fix migrations/env.py` reordered them to `from app.infrastructure.db import models as _models` before `from app.infrastructure.db.session import Base`. Effective behavior is identical because `models.py` imports `Base` from `session.py` internally.
- **Files modified:** `migrations/env.py`
- **Verification:** `uv run pytest tests/integration/test_alembic_metadata.py` still passes (Base.metadata contains all four tables); `make check` green.
- **Committed in:** `9bb98f5` (Task 2 commit — the env.py lint fix was bundled with the Task 2 work).

---

**Total deviations:** 3 auto-fixed (2 Rule 1 bugs caused directly by our changes, 1 lint auto-fix from ruff).
**Impact on plan:** All fixes essential for correctness (tests passing) or for the same-file linting the project enforces. No scope creep — nothing outside the seven task-owned files was touched beyond the one-line stale-assertion fix in the Phase 1 smoke test.

## Issues Encountered

- **Alembic autogenerate refused to run initially** — `Target database is not up to date.` The dev `dojo.db` at repo root was still at the empty state. Ran `uv run alembic upgrade head` against it first (brought it to 0001), then the autogenerate call emitted the expected 4 `Detected added table` log lines. Removed the dev DB file after confirming round-trip works end-to-end.
- **`_migrated_engine` not transitively requested** — see Deviation #2 above.

## Threat Model Discharge

The plan's threat register had 5 entries. Disposition status:

| Threat ID | Category | Disposition | Mitigation shipped |
|-----------|----------|-------------|--------------------|
| T-03-02-01 | Tampering (UUID→TEXT coercion) | mitigate | `test_source_round_trip` asserts bytewise equality via dataclass `__eq__`; `uuid.UUID(str(u))` is the stdlib inverse pair. |
| T-03-02-02 | Tampering (Card.tags JSON drift) | mitigate | `test_card_round_trip_with_tags` (populated) + `test_card_round_trip_empty_tags` (empty) prove both paths. |
| T-03-02-03 | DoS (migration could drop data) | accept | Local-first v1; 0001 baseline is empty; 0002 only creates. Downgrade reversibly drops. |
| T-03-02-04 | Tampering (SQLite-incompatible DDL) | mitigate | Human-review checklist cleared all 6 bullets; round-trip test defends future revisions. |
| T-03-02-05 | Info disclosure (M9 empty migration) | mitigate | env.py eager-import + 5-line metadata smoke test. |

No new threat surface introduced — no network endpoints, no auth paths, no file access outside SQLAlchemy's own DB I/O. No `threat_flag` section needed.

## Known Stubs

None. All data flows are wired end-to-end: mappers round-trip real fields, migration creates real tables, tests exercise real SQLAlchemy / real Alembic (not mocks).

## Next Phase Readiness

- **Plan 03 (SQL repositories)** can now build `Sql*Repository` adapters against these row classes. `session.merge(source_to_row(src))` on save; `session.get(SourceRow, str(source_id))` + `source_from_row(row)` on get. Exactly the glue-layer shape anticipated in RESEARCH §A.
- **Plan 05 (composition root + arch docs)** picks up the belt-and-suspenders `lazy="raise"` note in `docs/architecture/overview.md` §5 per D-02c.
- No blockers for downstream waves.

## Self-Check: PASSED

Created files verified on disk:
- `app/infrastructure/db/models.py` — FOUND
- `app/infrastructure/db/mappers.py` — FOUND
- `migrations/versions/0002_create_initial_schema.py` — FOUND
- `tests/unit/test_mappers.py` — FOUND
- `tests/integration/test_alembic_metadata.py` — FOUND
- `tests/integration/test_alembic_round_trip.py` — FOUND

Commits verified in `git log --oneline`:
- `cd20962` — FOUND (Task 1)
- `9bb98f5` — FOUND (Task 2)
- `148ac67` — FOUND (Task 3)

Gate verifications:
- `uv run pytest tests/integration/test_alembic_metadata.py tests/unit/test_mappers.py tests/integration/test_alembic_round_trip.py -x` — 8 passed.
- `uv run make check` — 102 passed, 1 LLM-opt-in skipped.
- `uv run lint-imports` — 3 contracts kept, 0 broken (domain stayed pure).

---
*Phase: 03-infrastructure-adapters*
*Plan: 02*
*Completed: 2026-04-24*
